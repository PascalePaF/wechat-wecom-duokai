# 微信 · 企业微信多开助手 V1.0.1 完整安全审计报告

文档编号：`WD-SA-2026-09-13-V101`

报告版本：2.0

审计日期：2026-09-12 至 2026-09-13（Asia/Tokyo）

审计范围：上游 GitHub 源码、用户取得的旧版 `duokai.exe`、本仓库 V1.0.0/V1.0.1 源码、构建链、安装/卸载逻辑和候选发布文件

## 1. 执行结论

### 1.1 最重要的结论

本次检查**没有发现原开源项目或本仓库存在后台上传隐私数据、窃取账号、读取聊天记录、远程控制、静默下载、开机自启、代码注入或隐藏载荷的证据**。

这个结论来自相互独立的多项证据，而不只是一次杀毒扫描：

1. 检查上游和当前仓库的全部 C#、项目文件、配置、资源声明与构建脚本；
2. 使用 ILSpy 反编译用户取得的旧版 EXE 和当前重新构建的 EXE；
3. 核对程序集类型、资源、托管依赖、原生导入、网址、注册表、文件和进程调用；
4. 对运行中的主程序和安装器观察子进程及 TCP/UDP 端点；
5. 使用本机 Kaspersky 对原始下载项目、当前二进制和最终发布目录执行关闭缓存加速的“全部文件”扫描；
6. 核对构建器签名、确定性重编译结果、Git 对象完整性、隐藏文件、备用数据流、长 Base64 数据和常见凭据模式。

### 1.2 关于卡巴斯基报毒

2026-09-12 23:43，Kaspersky 实时防护确实将一次中间构建识别为：

```text
VHO:Trojan.Win32.Convagent.gen
类型：木马
级别：高
方法：启发式分析
来源：云保护
```

这条记录是真实告警，不能直接忽略；但它也**不是已确认存在木马代码的证明**。Kaspersky 官方定义中，“启发式分析”用于发现尚未写入病毒库、行为或指令序列类似威胁的对象，被判定对象属于“可能感染”；官方也承认，非感染代码与病毒相似时会出现误报。KSN/云保护使用云端信誉和持续更新的知识库参与判断。[5][6]

本次告警后的源代码复核、反编译、依赖/资源核对、运行时网络观察和多次强制扫描都没有找到能支持“隐私上传”或“木马控制”的第二条证据。后续当前构建扫描为 0 检测、0 可疑。因此，现阶段最准确的表述是：

> **这是一条未被第二项证据证实的通用启发式/云端判定，误报可能性较高；但只有 Kaspersky 病毒实验室能够对该告警作最终误报裁定。**

### 1.3 整体风险评级

| 项目 | 评级 | 结论 |
| --- | --- | --- |
| 已确认恶意代码 | 未发现 | 无恶意链条或隐藏载荷证据 |
| 隐私上传 | 低 | 源码/反编译无网络上传实现；短时运行观察无网络端点 |
| 底层行为兼容性 | 中 | 句柄枚举和关闭使用 Windows 内部接口，客户端或系统升级可能失效 |
| 供应链与信誉 | 中 | 当前发布文件未使用商业代码签名；旧版 EXE 与公开源码不完全一致 |
| 误报概率 | 中至高 | 未签名的新哈希叠加句柄操作、锁文件处理和自删除清理器，容易触发信誉/启发式模型 |
| 完全卸载误删 | 低 | 有标记、结构、精确路径、链接拒绝和多重确认；仍不能抵御已控制同一用户账户的攻击者 |
| 当前审计置信度 | 中高 | 静态、反编译、运行和 Kaspersky 证据一致；受病毒库过期及缺少第二款最新引擎限制 |

## 2. 范围、基线与限制

### 2.1 审计对象

- 上游仓库：[CN-Root/wechat-wecom-duokai][1]，审计基线 `b9e3184d3a21a577e88a80c0e1de60a235844bf9`；GitHub 页面显示 4 个提交。
- 改进仓库：[PascalePaF/wechat-wecom-duokai][2]。
- 用户取得的旧版 `duokai.exe`：282,624 字节，SHA-256：`A4BD0344E17116592B7D6805F386D55CA26D0D17C849E76B8BA215D832499260`。
- 当前 V1.0.1 主程序、安装器、清理器、绿色版、发布脚本、测试和文档。
- 本机编译环境中的 MSBuild 与 C# 编译器。

### 2.2 未包含的保证

- 本报告不是形式化验证，不能证明未来版本或任何第三方重新打包文件安全。
- 运行观察只覆盖主界面和安装器空闲状态，未拦截微信自身的网络流量，也没有模拟所有用户点击路径。
- 未将本地样本上传到多引擎网站，避免未经确认向第三方发送文件。
- Microsoft Defender 因 Kaspersky 接管而处于禁用状态，没有形成第二款本机引擎的复扫证据。
- Kaspersky 报告其病毒库“已过期”；本次扫描显示完整库日期为 `2026-09-11 00:57:00`。尝试更新时所有更新源均发生 TLS 错误，更新任务失败。因此零检出是有效证据，但不能当作“最新病毒库零检出”。
- 告警的中间二进制已被 Kaspersky 删除并备份，本报告没有绕过防护将其恢复执行。

## 3. 审计方法与证据链

| 检查 | 方法 | 结果 |
| --- | --- | --- |
| Git 来源 | 远程地址、提交历史、标签、`git fsck --full`、子模块/LFS/跟踪二进制检查 | Git 对象完整；无子模块、无 LFS、源码树未跟踪 EXE/DLL/压缩载荷 |
| 源码静态检查 | 全仓库 API、URL、注册表、文件、进程、持久化、注入、凭据、长编码串检索 | 只发现本文列明的公开功能；无隐蔽网络或凭据代码 |
| 项目与构建检查 | `.csproj`、导入目标、构建脚本、资源项、预/后构建任务 | 无 NuGet/第三方包；无自定义 `Exec`、下载或远程构建步骤 |
| 反编译 | ILSpy 11.0 对旧版及当前 EXE 完整列举类型和资源 | 可完整反编译；未发现混淆、隐藏命名空间、额外模块或未知载荷 |
| PE/CLR 检查 | 依赖表、模块引用、原生导入、清单、ASLR/DEP 标志 | `asInvoker`、`uiAccess=false`；Dynamic Base、NX；主程序原生模块仅 DWM、Kernel32、Ntdll |
| 资源检查 | PNG/ICO 尺寸、格式、哈希、PNG IEND 尾部 | 图标可正常解析；PNG 结束块后无追加数据 |
| 密钥/数据流检查 | 常见 token/password/private-key 模式、长 Base64、备用数据流 | 未发现凭据；Base64 仅用于临时清理计划字段；无备用数据流 |
| 运行观察 | 启动候选主程序/安装器 3 秒，按精确 PID 观察 TCP、UDP、子进程和模块后正常关闭 | 两者 TCP=0、UDP=0、子进程=0；未触碰用户原先运行的旧版进程 |
| 确定性构建 | 相同源码连续 Release 重编译并比较 SHA-256 | 主程序和安装器两次哈希分别完全一致 |
| 杀毒检查 | Kaspersky 全文件、仅报告、关闭 iChecker/iSwift | 结果和限制见第 8 节 |

## 4. 上游源码：有没有后台上传隐私

### 4.1 实际行为

上游原始程序的有效行为只有：

- 从 `HKCU` 注册表读取微信、企业微信安装路径；
- 向 `HKCU\SOFTWARE\Tencent\WXWork` 写入 `multi_instances`；
- 按用户输入的数量启动微信或企业微信 EXE；
- 只有用户主动点击界面链接时，才打开 `https://www.root.tax/`；
- 使用 WinForms 计时器连续发送启动请求。

### 4.2 未发现的行为

完整源码中未发现：

- `HttpClient`、`WebClient`、`WebRequest`、TCP/UDP/Socket、WebSocket、FTP 或上传/下载实现；
- 读取微信聊天数据库、消息、账号、Cookie、浏览器密码、Windows 凭据库、剪贴板、摄像头或麦克风；
- 键盘钩子、屏幕抓取、远程桌面或命令控制；
- `Run`/`RunOnce`、计划任务、服务、驱动、WMI 持久化；
- PowerShell、`cmd.exe`、隐藏脚本、动态程序集加载或反射发射；
- `CreateRemoteThread`、`WriteProcessMemory`、`VirtualAllocEx`、DLL 注入或客户端二进制补丁；
- 加密配置、硬编码服务器、API 密钥、访问令牌或长编码载荷。

上游 `.csproj` 虽然列出了未使用的 `System.Net.Http` 引用，但编译出的程序集引用表并没有 `System.Net.Http`，源代码也没有调用它。单独存在一个未使用的框架引用不能实现联网。

### 4.3 对“上传隐私”的结论

网络上传至少需要网络 API、外部进程/脚本、嵌入组件或动态加载中的一种数据出口。本次在源码、旧版反编译、当前反编译、资源、依赖和空闲运行端点中均未找到该出口；也没有找到待上传的数据采集入口。因此：

> **没有证据表明原开源源码会后台上传隐私数据。**

这不覆盖被第三方替换的同名 EXE，也不等于对未来提交作保证。使用时应核对仓库来源和 Release 哈希。

## 5. 用户取得的旧版 EXE

### 5.1 文件身份

| 属性 | 值 |
| --- | --- |
| 文件名 | `duokai.exe` |
| 大小 | 282,624 字节 |
| 产品名 | 微信&企业微信双开神器 |
| 文件版本 | 1.0.0.0 |
| SHA-256 | `A4BD0344E17116592B7D6805F386D55CA26D0D17C849E76B8BA215D832499260` |
| SHA-1 | `A2C4369D9643F2687C3118EE102E6B3AB465E3C7` |
| Authenticode | 未签名 |
| 混淆 | 未发现；ILSpy 可完整还原 |

### 5.2 与公开源码不一致

旧版 EXE 内部固定了：

```text
D:\中文安装地址\微信\Weixin\Weixin.exe
```

而上游 GitHub 源码通过注册表拼接微信路径。反编译类型清单只有 `shuangkai.Home`、`shuangkai.Program` 和标准资源/设置类，没有隐藏的额外业务类型；可见逻辑仍只有注册表、启动客户端和手动打开作者网站。

因此，这个差异首先是**可复现性和来源风险**：它来自一份未公开保存的本地修改源码，不能只凭 GitHub 提交证明二进制身份。它不是后门证据，但也是不继续分发旧 EXE 的充分理由。

## 6. 当前 V1.0.1 的敏感能力

### 6.1 为什么会使用底层句柄 API

“已经开 3 个、误关 1 个后只补回 1 个”要求在仍有客户端运行时解除单实例限制。当前实现会：

1. 枚举当前 Windows 会话中的微信/企业微信进程；
2. 严格核对运行进程路径必须等于已定位的客户端路径，读取失败时直接跳过；
3. 使用 `NtQuerySystemInformation` 枚举系统句柄；
4. 只以 `PROCESS_DUP_HANDLE` 打开候选进程；
5. 复制句柄到本进程读取类型/名称；
6. 只关闭白名单互斥锁，或精确指向 `%APPDATA%\Tencent\xwechat\lock\lock.ini` 的文件句柄；
7. 随后只启动“目标数量 - 当前数量”的缺失实例。

`DuplicateHandle` 官方文档明确说明 `DUPLICATE_CLOSE_SOURCE` 会关闭源句柄。[3] 这类能力本身不等于恶意注入，但与安全工具、调试器和恶意软件都可能使用的底层技术有表面相似性，是启发式模型重点关注的合理原因。

`NtQuerySystemInformation` 是 Windows 内部接口。Microsoft 明确警告它可能在未来版本改变或不可用，并建议使用替代函数/运行时动态链接。[4] 当前代码有失败保护，但兼容性风险仍存在。

### 6.2 本轮审计后的进一步收紧

- 进程路径无法读取时不再仅凭 `Weixin.exe`/`WXWork.exe` 名称接受，改为失败即跳过；
- `rundll32` 兼容候选只允许匹配精确 `lock.ini` 文件路径，禁止对其应用互斥锁名称匹配；
- 只有 `DuplicateHandle(...DUPLICATE_CLOSE_SOURCE)` 真正成功才计入关闭数量；
- 句柄目标仍限定为当前 Windows 会话，不扫描其他登录会话；
- 不申请调试权限，不提升到管理员，不终止微信/企业微信，不写客户端内存。

### 6.3 明确没有做什么

- 不注入 DLL，不创建远程线程；
- 不修改或替换腾讯客户端文件；
- 不破解登录、不绕过账号认证；
- 不结束微信、企业微信或其后台；
- 不读取聊天数据；
- 不创建网络监听端口；
- 不后台上传任何程序状态。

## 7. 安装、配置与完全卸载审计

### 7.1 写入范围

| 位置 | 内容 | 是否需要管理员 |
| --- | --- | --- |
| 用户选择的专用安装目录 | 主程序、卸载器、配置、许可证、安装标记 | 否（目标目录须可写） |
| `%LOCALAPPDATA%\WechatDuokai` | `settings.ini`、`theme.ini`、用户数据标记 | 否 |
| 当前用户开始菜单/可选桌面 | 两个快捷方式 | 否 |
| `HKCU\...\Uninstall\WechatDuokai` | 当前用户卸载登记和已验证路径 | 否 |
| `HKCU\SOFTWARE\Tencent\WXWork\multi_instances` | 企业微信多开数量提示 | 否 |

主程序和安装器清单均为 `requestedExecutionLevel=asInvoker`、`uiAccess=false`，不会静默索要管理员权限。

### 7.2 删除范围与保护

“保留源码”选项可删除已验证的安装目录、用户设置、开始菜单/桌面快捷方式、发布目录和绿色包；“全部删除”才会尝试删除源码。保护措施包括：

- 安装目标拒绝磁盘根目录、Windows/系统/用户资料关键目录、源码/发布/绿色目录和未标记的非空目录；
- 源码、安装、发布、绿色和用户数据各使用不同标记；
- 标记目录还必须通过对应结构检查，标记不是唯一条件；
- 所有作为递归删除根的目录都拒绝 `ReparsePoint`；
- 清理时逐层遍历，遇到目录联接/符号链接只删除链接本身，不进入链接目标；
- 清理计划必须与当前工作进程和计划文件路径一致；
- “删除源码”默认未选中，要求额外复选确认和最后一次对话框确认；
- 清理器只关闭路径位于已验证项目目录中的本工具进程，不结束微信或企业微信。

### 7.3 剩余边界

目录标记用于防止**误删**，不是密码或数字签名。已经完全控制同一 Windows 用户账户的攻击者，本来就能修改这些文件、注册表和用户可写目录；公开标记无法抵御这种攻击者。安装路径在校验与实际写入之间也存在很短的同用户竞态窗口。商业代码签名和更强的安装技术可以进一步改善供应链身份，但不能代替操作系统账户安全。

## 8. Kaspersky 告警时间线与扫描结果

### 8.1 告警时间线

| 时间 | 对象 | 事件 |
| --- | --- | --- |
| 2026-09-12 23:43:02 | `duokai\obj\Release\duokai.exe` | `VHO:Trojan.Win32.Convagent.gen`，高，启发式分析，发起者 MSBuild，云保护 |
| 2026-09-12 23:43:04 | 测试目录安装器内嵌 `data0000.res` | 同一判定；该资源就是当时的主程序载荷 |
| 2026-09-12 23:44:39 | 上述 OBJ 中间文件 | Kaspersky 删除并创建备份副本 |
| 2026-09-12 23:49:24 | 后续当前主程序与安装器 | 强制全文件扫描：3 正常、0 检测、0 可疑 |
| 2026-09-13 00:03:01–00:03:07 | 原始下载项目完整目录 | 62 正常、0 检测、0 可疑、0 跳过、0 错误 |
| 2026-09-13 00:08:11–00:08:12 | 重新构建主程序与安装器 | 3 正常、0 检测、0 可疑、0 跳过、0 错误 |
| 2026-09-13 发布门禁 | 安装版、清理器、绿色目录和 ZIP 的完整发布目录 | 30 正常、0 检测、0 可疑、0 跳过、0 错误；完整时间以 Release 扫描日志为准 |

安装器被计为 3 个扫描对象，是因为 Kaspersky 同时检查外层安装器、内嵌 .NET 资源和主程序载荷，不代表存在三个程序。

### 8.2 “高危、启发式、云监测”分别是什么意思

- **高危/高**：如果该判断为真，产品把潜在后果归类为严重；它不是“判断置信度 100%”。
- **启发式分析**：未必命中已知样本的精确签名，而是代码结构、指令组合或行为特征类似威胁。Kaspersky 官方把这类对象描述为“可能感染”。[5]
- **云监测/云保护**：KSN 云端信誉、机器学习或快速更新的知识库参与判断。它能更快识别新威胁，也可能因为新文件缺乏信誉而发生波动。[5]
- **`Convagent.gen`**：`gen` 表明通用/泛化家族式判定。审计时没有在 Kaspersky 官方威胁百科中找到能把这个名称唯一映射到本项目具体恶意行为的条目，因此不能仅凭名称反推“它上传了聊天记录”。

### 8.3 为什么目前不能直接写“已经确认误报”

后续零检出、源码透明、无网络/隐私代码和可复现构建都强烈削弱了“真实木马”的假设，但最终裁定仍需把被判定样本和完整事件日志交给 Kaspersky 病毒实验室。Kaspersky 官方支持材料也把“非感染对象因代码相似而被判感染”定义为误报。[5][6]

### 8.4 病毒库状态限制

- 扫描日志显示完整库日期：`2026-09-11 00:57:00`；
- 产品事件持续提示“数据库已过期”；
- 2026-09-12 23:54–23:55 主动更新连接多个官方更新节点时均返回 TLS 错误；`Updater` 最终为 `failed`；
- 系统 DNS/HTTPS 测试同样出现 TLS 握手失败，说明更新失败至少与本机网络/TLS 路径有关，不能据此推断本项目阻止更新。

因此发布扫描日志必须同时公开病毒库日期，不能只展示“0 检测”。

## 9. 二进制、依赖与构建链结果

### 9.1 当前主程序

- 托管程序集引用实际只有：`mscorlib`、`System`、`System.Core`、`System.Drawing`、`System.Windows.Forms`；
- 原生模块引用只有：`dwmapi.dll`、`kernel32.dll`、`ntdll.dll`；
- 嵌入资源只有窗口图标、微信图标和企业微信图标；
- 没有第三方 DLL、NuGet 包、压缩脚本、加密载荷或网络组件；
- PE 启用 Dynamic Base、High Entropy VA、NX Compatible；
- 未签名。

### 9.2 当前安装/清理器

- 托管引用只有标准 .NET Framework UI/核心程序集；
- 原生模块引用只有 DWM 和 Kernel32（自删除使用 `MoveFileEx`）；
- 嵌入资源明确为主程序、主程序配置和 Apache-2.0 许可证；
- 不下载载荷，安装内容全部来自构建时嵌入资源；
- 未签名。

### 9.3 构建链

- MSBuild、Windows .NET Framework C# 编译器和 Visual Studio Roslyn 编译器的 Authenticode 均为有效 Microsoft 签名；
- `.csproj` 没有预构建/后构建命令、`Exec` 或自定义 `UsingTask`；
- `build-release.ps1` 只执行本地重编译、测试、复制、压缩和 SHA-256 生成；
- 相同源码连续构建产生相同主程序和安装器 SHA-256，确定性验证通过；
- Git 完整性检查无错误，仓库没有子模块、LFS 或已跟踪可执行载荷。

## 10. 发现项与处置

| 编号 | 发现 | 影响 | 可能性 | 当前处置 |
| --- | --- | --- | --- | --- |
| F-01 | 未签名、每次版本产生新哈希 | 信誉告警、无法验证发布者 | 高 | 公开源码/哈希/扫描；代码签名列为最高优先级 |
| F-02 | 使用系统句柄枚举和关闭 | AV 启发式告警、客户端兼容性 | 中高 | 严格 PID/路径/锁白名单、失败保护、测试覆盖 |
| F-03 | `NtQuerySystemInformation` 为内部 API | Windows 更新后失效 | 中 | 失败时不破坏客户端；后续优先寻找官方多开接口 |
| F-04 | 客户端路径来自用户可写注册表/进程/常见目录 | 本机账户已受控时可被替换 | 低至中 | 仅允许预期文件名并显示路径；建议增加腾讯签名验证 |
| F-05 | 企业微信 `multi_instances` 会保留 | 改变当前用户客户端配置 | 低 | 文档公开；建议后续保存并恢复原值 |
| F-06 | 完全卸载包含递归删除 | 误操作可能丢失未提交源码 | 中 | 多重标记、结构、链接拒绝、二次确认；默认保留源码 |
| F-07 | 标记值公开且清理计划无密码学签名 | 不能抵御同账户恶意篡改 | 低 | 权限不越过当前用户；建议未来使用签名清单/安装数据库 |
| F-08 | 旧版 EXE 与上游源码不一致 | 来源不可复现 | 中 | 不再分发旧 EXE；只发布当前仓库可重建版本 |
| F-09 | Kaspersky 库过期且更新失败 | 零检出证据强度下降 | 中 | 报告透明披露；需网络恢复后更新并再次扫描 |
| F-10 | 多开属于客户端非标准兼容技巧 | 登录冲突、账号/组织策略风险 | 中 | 不修改客户端；建议先用测试账号并遵守许可/组织规则 |

## 11. 是否可以运行

### 个人本机测试

在只从本仓库 Release 下载、核对 SHA-256、最终扫描仍为 0 检测，并理解多开兼容性风险的前提下，现有证据支持进行个人本机测试。首次测试建议使用非关键账号，不要关闭安全软件，也不要把程序加入永久白名单。

### 企业或大规模分发

在完成以下事项前，不建议当作企业级可信软件大规模部署：

1. 使用受信任的 Authenticode 代码签名证书并加 RFC 3161 时间戳；
2. 在病毒库成功更新后重扫；
3. 使用独立的第二款最新安全引擎复核；
4. 把告警样本提交 Kaspersky 病毒实验室取得正式裁定；
5. 加入腾讯客户端签名验证和企业微信配置恢复；
6. 为句柄操作建立可关闭的“兼容模式”，默认优先官方支持方式。

Microsoft 说明，Authenticode 为二进制提供作者身份与完整性保证；时间戳能让签名在证书过期后继续验证。[8] Microsoft 也说明，未签名文件每个新版本都必须从零建立哈希信誉，容易出现 SmartScreen 未知发布者提示。[7]

## 12. 后续安全优化优先级

### P0：本次发布门禁

- 最终 Release 目录已执行 Kaspersky 全文件扫描，结果 30 正常、0 检测、0 可疑；只有复扫继续为 0 才发布；
- 发布 `SHA256SUMS.txt`、扫描日志、源码提交和标签；
- Release 说明中公开本次告警、后续结果、病毒库日期和限制；
- 不关闭卡巴斯基、不恢复告警样本运行、不通过白名单掩盖问题。

### P1：下一版本优先

- 为主程序、安装器和清理器购买/接入 Authenticode 签名；
- 启动前验证微信/企业微信签名链和发布者为腾讯，并允许用户查看验证结果；
- 保存企业微信 `multi_instances` 原值，完成启动请求后恢复；
- 将底层锁解除放入显式“高级兼容模式”，显示将要处理的进程路径和锁类型；
- 调查腾讯当前客户端是否存在官方多开参数/API，能替代内部 NT 句柄枚举时立即迁移。

### P2：工程化

- 增加 CI 干净环境构建、单元测试、SBOM、源码提交/编译器版本/产物哈希证明；
- 增加目录联接、锁名碰撞、PID 重用、路径替换和断电清理测试；
- 对每个 Release 保存可复现构建说明和两款独立扫描结果；
- 建立 `SECURITY.md` 漏洞报告渠道，不要求报告者公开敏感样本。

## 13. 最终意见

1. **原开源源码上传隐私：未发现证据。** 没有网络数据出口，也没有聊天/凭据采集入口。
2. **旧版 EXE：未发现后门，但不可复现且未签名。** 不建议继续使用或分发。
3. **Kaspersky 告警：真实发生，但尚未被证实为真实木马。** 通用启发式/云端判断与底层句柄操作、未签名新哈希之间存在合理关联；只有 Kaspersky 实验室能确认误报。
4. **当前 V1.0.1：源码和后续二进制检查未发现恶意链条。** 已进一步缩窄进程/句柄/删除范围。
5. **不能宣称绝对安全。** 未签名、内部 NT API、病毒库过期和缺少第二引擎仍是必须公开的剩余风险。

## 参考资料

[1]: https://github.com/CN-Root/wechat-wecom-duokai "CN-Root/wechat-wecom-duokai"
[2]: https://github.com/PascalePaF/wechat-wecom-duokai "PascalePaF/wechat-wecom-duokai"
[3]: https://learn.microsoft.com/en-us/windows/win32/api/handleapi/nf-handleapi-duplicatehandle "Microsoft Learn — DuplicateHandle"
[4]: https://learn.microsoft.com/en-us/windows/win32/api/winternl/nf-winternl-ntquerysysteminformation "Microsoft Learn — NtQuerySystemInformation"
[5]: https://support.kaspersky.com/help/kaspersky/win21.5/en-us/90.htm "Kaspersky Glossary — heuristic analyzer, KSN, false positive"
[6]: https://support.kaspersky.com/ksc/14/en-US/158852.htm "Kaspersky Security Center — file statuses and false positives"
[7]: https://learn.microsoft.com/en-us/windows/apps/package-and-deploy/smartscreen-reputation "Microsoft Learn — SmartScreen reputation for app developers"
[8]: https://learn.microsoft.com/en-us/windows/win32/seccrypto/time-stamping-authenticode-signatures "Microsoft Learn — Time Stamping Authenticode Signatures"

---

本报告采用“发现了什么、没有发现什么、还不能证明什么”三层表述。杀毒零检出是证据，不是证明；启发式告警是风险信号，也不是自动等同于后门。
