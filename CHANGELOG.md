# 更新记录

## V1.1.6 — 2026-09-23

- 自动更新检查从“仅启动时判断”改为程序持续运行期间按本地计划继续检查；设置关闭后不发起未执行的检查，重新开启时检查是否已到期。保留错峰、24 小时缓存与失败退避。
- 开机启动改为按当前副本及其记录的旧路径判断归属。安装版和绿色版共存时不会互删启动项；切换到另一份程序必须经用户明确确认，确认期间注册表值变化则拒绝覆盖。
- 第二次点击快捷方式会向已运行实例发送当前会话信号，恢复开机启动后最小化的窗口。
- 设置文件改用同目录写穿暂存、替换和备份；主文件缺失时可读取备份，避免删除旧文件后断电造成配置丢失。
- 普通安装也使用暂存、逐文件备份及故障回滚。中断标记防止半安装程序启动；重新运行正式安装包可以在原目录完成安装。
- 增加持续运行 48 小时调度、双副本及确认式启动项切换、备份恢复、窗口唤回和安装多步故障注入测试；保留 14 场景 UI 回归。
- 开放 GitHub Issues，并提供故障与功能建议表单；公开提交前提示先遮盖账号、聊天与本地敏感路径。

## V1.1.5 — 2026-09-22

- 设置新增当前 Windows 用户的开机自动启动开关，以及“自动启动时最小化”；启动项只运行助手，不会
  自动打开微信或企业微信，也不需要管理员权限。
- 启动项固定指向带正式标记目录中的 `wechat_duokai.exe --autostart`。绿色版移动后自动修复本程序
  拥有的旧路径；同名值不符合精确格式时视为外部冲突并完整保留。
- 清理器只在启动项解析出的 EXE 与已验证安装/绿色目录完全一致时删除该值；第三方冲突值及其他程序
  启动项不受影响。重复的自动启动进程检测到现有实例时静默退出，不弹打扰提示。
- 设置新增“发现后自动下载并校验（安装前确认）”。自动流程只准备本项目 GitHub Release 的正式
  setup；不会自动执行安装程序，最终覆盖前仍必须由用户确认。
- 更新下载新增可信缓存复用：静态清单/GitHub 摘要、`SHA256SUMS.txt`、文件大小和本机 setup 哈希
  全部一致才复用；缓存损坏时只删除该版本两个已知文件并重新下载，启动 setup 前再次计算 SHA-256。
- 设置页显示上次成功检查与下次自动检查时间；保留静态清单优先、可选 API 交叉核验、30–300 秒错峰、
  24 小时成功缓存、1/6/24 小时失败退避与代理兼容策略。
- 最小 901×513 设置页重新压缩信息层级，日间/夜间均无裁切；诊断报告补充启动和更新偏好，但不记录
  启动命令、账号、聊天、代理凭据或访问令牌。
- 新增启动项移动修复、外部冲突、精确卸载、自动启动参数、设置持久化、检查计划展示、更新缓存复用
  和篡改拒绝回归测试；继续生成 14 场景 UI 回归样本、哈希与 SPDX 2.2 SBOM。

## V1.1.4 — 2026-09-21

- 按用户截图和仓库 V1.0.10 日间/夜间基线，恢复右侧“目标窗口数”卡片的原始内部结构。
- 标题下恢复“微信与企微共用”；底部从“●  两端共用 · 自动保存”恢复为“●  数量会自动保存”。
- 数量卡片恢复 `Auto / 18 / 76 / 12 / Auto` 五段结构并整体垂直居中，修复 V1.1.3 中部偏空、
  状态条过低的问题。
- 左右主卡片外框继续同顶同底；应用卡片仍使用四段比例轨道，全部退出、启动/补开、页脚计数和设置
  功能均保持不变。
- UI 回归改为同时断言外框对齐和 V1.0.10 数量卡片结构，并重新生成 14 个日间/夜间、多分辨率样本。
- 安装覆盖检测测试会等待进程句柄释放并重试清理，避免 Windows 安全软件短暂占用临时 EXE 造成偶发误报。
- V1.1.3 的静态清单、环境代理、TLS 1.2、多重 SHA-256、事务回滚和不可变 Release 边界保持不变。

## V1.1.3 — 2026-09-20

- 修复系统代理关闭、但当前进程通过 `HTTPS_PROXY` / `HTTP_PROXY` 环境变量联网时，浏览器和 curl 可访问
  GitHub、.NET Framework 4.8 客户端却可能超时或 TLS 失败的问题。
- 新增统一网络策略：优先采用合法 `HTTPS_PROXY`，为空时采用 `HTTP_PROXY`；版本发现、校验文件与 setup
  下载使用同一代理，不把代理地址或凭据写入设置、诊断或日志。
- 代理 URI 只接受 `http` / `https`、主机、端口与根路径；拒绝文件 URI、额外路径、query 和 fragment，
  URL user-info 只作为本地代理凭据。
- GitHub 请求固定使用 TLS 1.2；静态清单首步由 GET 改为无正文 HEAD，再从规范版本地址下载受 64 KiB
  上限保护的 JSON。
- 在 `HTTPS_PROXY=http://127.0.0.1:7897`、WinHTTP Direct、Internet ProxyEnable=0 的实机环境中，以
  .NET Framework 4.8 完成版本发现、API digest 交叉核验、SHA256SUMS/setup 下载与最终 SHA-256 校验。
- 增加合法/非法代理 URI、凭据解析和静态清单 HEAD 跳转回归；静态清单、多重摘要、用户确认、事务回滚
  与不可变 Release 边界保持不变。
- 更新状态测试清理会短暂等待实时安全扫描器释放其临时文件，避免把外部瞬时占用误报成产品回归失败。

## V1.1.2 — 2026-09-20

- 每个正式 Release 新增 `update-manifest.json`，客户端从固定 `latest/download` 地址取得版本、规范附件
  URL、精确大小和 SHA-256；检查和一键更新不再以公共 REST API 可用为前提。
- GitHub API 降为发现新版本后的可选摘要交叉核验。API 403、限流、超时或共享出口配额耗尽时，仍可用
  静态清单、`SHA256SUMS.txt` 与本机下载文件完成多重校验；元数据冲突则停止一键更新。
- 自动检查成功后缓存 24 小时；按安装目录生成不会上传的稳定 30–300 秒启动错峰。连续失败按 1 小时、
  6 小时、24 小时退避，并识别 `Retry-After` / `X-RateLimit-Reset`，总抑制时间封顶 24 小时。
- 用户手动“检查更新”始终立即发起，不受缓存、错峰或退避影响；一键安装仍必须由用户确认，不会静默
  下载或覆盖。
- 新增安装目录内 `data\update-state.ini`，只保存检查时间、失败次数和最近版本；使用写穿临时文件和
  原子替换，不保存账号、聊天、诊断、设备标识或访问令牌。
- 构建脚本在二进制与 `SHA256SUMS.txt` 完成后生成静态清单；GitHub Actions 重新核对清单中的 setup、
  校验文件和绿色 ZIP 的名称、规范 URL、大小与 SHA-256，再生成和验证 SPDX 2.2 SBOM。
- 仓库启用 GitHub Immutable Releases；V1.1.2 起所有附件先上传草稿，正式发布后标签和附件不可替换。
- 增加静态清单信任边界、API 403 可用性、旧 Release 兼容、24 小时缓存、稳定错峰、1/6/24 小时退避、
  Retry-After 和损坏远未来状态的自动回归。

## V1.1.1 — 2026-09-20

- 定位并修复“检查失败”：V1.0.11 直接请求 GitHub 未登录 REST API；共享出口每小时 60 次配额耗尽后，
  GitHub 返回 HTTP 403，界面又隐藏了底层原因。
- 最新正式版本改为先从 `github.com/.../releases/latest` 的可信跳转解析，不消耗 REST API 配额；
  当前已是最新版时不再访问 `api.github.com`。
- 发现新版本后仍优先读取 GitHub 附件 digest；API 限流、超时或连接失败时，自动改用精确仓库、标签、
  附件名的 HTTPS Release 地址，并通过 HEAD 校验最终 GitHub 交付域、文件存在性和大小上限。
- 用户确认更新后才下载 `SHA256SUMS.txt` 与 setup。API 可用时继续执行三方 SHA-256；API 不可用时
  强制清单与下载文件 SHA-256 一致，所有暂存、备份和回滚边界不变。
- 修正 HttpClient 自身超时会被误当作用户取消、导致界面停留在“正在检查”的异常分支；真正失败时显示
  超时、连接失败、非可信地址或无法识别版本等具体原因。
- 新增 GitHub API 403 / `X-RateLimit-Remaining: 0` 回归用例，并在本机真实限流状态下验证仍可识别
  V1.1.0、setup 精确大小和 `SHA256SUMS.txt`。

## V1.1.0 — 2026-09-20

- 微信与企业微信卡片各新增一个低干扰“全部退出”按钮；确认后只处理当前 Windows 会话中、路径与已验证官方客户端完全一致的进程。
- 退出流程先向匹配进程请求正常关闭，等待后才结束仍残留的匹配进程；路径无法读取、不同会话或 PID 已变化时一律跳过。
- 左右主卡片改用相同的四行比例结构，标题、操作区、状态区及外框上下边界在最小、高清和超宽窗口中保持对齐。
- 左下角两类实时窗口数改为独立的绿/蓝粗体数字，并增加左右留白；两行仍保持左对齐且每 2 秒刷新。
- 移除左上角“微信 · 企业微信”副标题；目标数量说明压缩为“微信与企微共用 · 自动保存”，主界面更简洁。
- 增加精确路径/当前会话退出边界、按钮与页脚强调、布局对齐等自动测试，并更新 14 场景双主题 UI 回归资料。
- 同步 README、安装版/绿色版说明、完整安全审计、一键更新验证、发布清单与 Kaspersky 扫描门禁。

## V1.0.11 — 2026-09-19

- 主程序、安装器与清理器窗口显式绑定微信绿 / 企微蓝 Logo，修复标题栏与任务栏可回退旧图标的问题。
- 为主程序、setup 和 cleanup 设置独立稳定 AppUserModelID，快捷方式写入匹配的 `System.AppUserModel.ID`；安装、更新、绿色启动和清理后通知 Explorer 刷新图标缓存。
- 新增窗口 `Icon` 和 app/setup/cleanup 原生 EXE 绿蓝像素自动测试，防止任务栏图标再次回归。
- 版本号收拢到 `Directory.Build.props`；界面、程序集、安装包名和更新 User-Agent 自动取值，发布脚本增加构建前后一致性门禁。
- 主程序、安装器和清理器遇到未处理 UI 异常时，提示后有序退出，不再在不确定状态下继续运行。
- 安装版也在所选程序目录保留 README、安全审计、版本说明、更新验证与本轮全项目自查报告。
- 完成网络、隐私、进程、句柄、注册表、文件删除、更新链、构建与供应链全项目自查，未发现后台上传或恶意代码。

## V1.0.10 — 2026-09-19

- 产品名称重新确定为“微窗助手”；窗口、安装器、清理器、文件属性、诊断报告与系统快捷方式统一更新。
- 重新设计原创双窗口 Logo，使用微信同类绿、企微同类蓝与薄荷连接色，提供 SVG、透明 PNG 和
  16–256 像素 Windows ICO。
- 日间主题移除金棕色，改为清爽的浅灰绿背景、深微信绿主操作和企微蓝信息色。
- 夜间主题切换为深绿蓝灰体系，主绿、信息蓝、图标底色、边框和选择色统一压暗，继续避免刺眼浅色。
- 覆盖安装和一键更新新增 V1.0.9“开开助手”快捷方式迁移，同时保留更早长名称的精确安全清理。
- 更新 14 场景 UI 回归、品牌色自动断言、README、发布说明、安全审计与一键更新验证报告。
- 多开核心、腾讯签名校验、企业微信注册表事务保护、程序目录数据边界和三方 SHA-256 更新链保持不变。

## V1.0.9 — 2026-09-19

- 产品正式命名为“开开助手”；主窗口、安装器、清理器、文件属性、诊断报告和系统快捷方式统一更新。
- 设计全新品牌 Logo：交叠圆角窗口表达多开与补开，提供 SVG、透明 PNG 和 16–256 像素 Windows ICO。
- 主界面移除五处低频说明，缩短标题区与页脚，重新平衡应用卡片和共用目标数量卡片。
- 设置页新增“常规 / 软件介绍”切换；从主界面移走的说明与三步使用方法集中保留在软件介绍中。
- 覆盖安装安全迁移旧名称快捷方式；内部 EXE、安装目录、标记、卸载键、配置和更新协议保持兼容。
- README 重写为下载、安装、第一次使用、补开、手动选择客户端、诊断与更新的完整教程。
- UI 回归矩阵扩展为 14 个场景，新增软件介绍页日间/夜间截图和品牌资源自动断言。
- 多开核心、腾讯签名校验、企业微信注册表恢复、程序目录数据边界和三方 SHA-256 更新链保持不变。

## V1.0.8 — 2026-09-19

- 夜间主题重做为低亮度、低饱和度配色；品牌金、成功绿、信息蓝、警告/危险色、正文和边框统一压暗。
- 微信与企业微信图标外层由写死的浅色改为动态主题资源，夜间使用深墨绿与深蓝灰背景，消除刺眼方块。
- 主界面页脚移除“诊断”和“发布版本”，右下角只保留“设置”。
- 设置页左侧新增“本地诊断 / 生成报告”，右侧保留发布页、检查更新和一键更新，低频工具集中管理。
- 新增夜间关键颜色亮度和实际图标资源绑定自动测试，并更新 12 场景日间/夜间 UI 回归矩阵。
- 核心多开、共用目标数、企业微信注册表事务保护、程序目录数据边界和三方 SHA-256 更新链保持不变。

## V1.0.7 — 2026-09-16

- 设置页新增由用户明确确认的一键更新；没有可信新版本时不显示安装按钮，不下载任何文件。
- 更新只接受本项目正式 GitHub Release 的精确 setup 附件，并强制 GitHub `digest`、
  `SHA256SUMS.txt` 与下载文件三方 SHA-256 一致。
- 最终哈希计算到启动安装器期间保持包文件不可写；独立安装器再次验证路径、哈希、版本、
  计划时效、父进程和目标目录身份。
- 安装版与绿色版均使用同卷暂存、逐文件备份和逆序回滚的事务更新；`data` 不进入替换列表。
- 目标窗口数按用户反馈恢复为微信与企业微信共用的一个 1–10 控件。升级时优先沿用
  V1.0.6 的微信目标值；若缺失再沿用企业微信值，随后仅保存单一配置项。
- 更新计划、下载、暂存、备份和结果日志全部位于程序目录的 `data`；不上传诊断、路径或日志。
- 自动测试新增 Release 附件信任边界、安装/绿色身份识别、替换中断回滚、设置保留和
  V1.0.6 双数量向共用数量迁移。

## V1.0.6 — 2026-09-16

- 微信与企业微信目标窗口数拆分为两个独立的 1–10 控件和配置项；旧版单一数量在迁移时同时作为两者初值。
- 企业微信注册表兼容会话在修改 `multi_instances` 前，原子写入包含事务号、进程身份、原始状态与临时
  状态的恢复日志，并使用写穿透和 `Flush(true)` 要求数据先落盘。
- 主程序每次启动先检查未完成事务：旧进程已退出且当前注册表仍等于临时状态时恢复原始存在性、值和
  `RegistryValueKind`；第三方已经写入新状态时保留第三方值并关闭旧事务。
- 新增有界本地恢复审计日志；不记录原注册表内容，只记录事务号、结果、原因和时间，超过 256 KiB 时
  轮换一份旧日志。
- 配置、主题、诊断、恢复事务和恢复审计统一迁移到程序目录的 `data` 子目录；主程序拒绝将数据目录
  指向符号链接或目录联接，并在目录不可写时明确停止启动。
- 安装器在所选安装目录内预建带安全标记的 `data`，升级时迁移旧 `%LOCALAPPDATA%\WechatDuokai`
  设置；绿色版首次启动执行相同迁移，此后不再向旧目录写入。
- 诊断目录调整为 `data\diagnostics`；便携版说明、清理逻辑、发布清单与完整安全审计同步更新。
- 自动测试新增真实子进程不调用 `Dispose` 的异常退出模拟、双开/扩展模式启动恢复、第三方冲突保留、
  `REG_MULTI_SZ` 精确往返、双目标独立缓存和全体持久路径约束。
- 保持 901×513 最小尺寸和无滚动条比例布局；右侧数量卡片在日间、夜间、高清和超宽屏下容纳两组控件。

## V1.0.5 — 2026-09-16

- 主窗口左下角新增两行实时根实例计数：“当前微信窗口 x 个”和“当前企业微信窗口 x 个”；每 2 秒刷新，
  启动流程进行中及结束后也会更新。
- 右下角最右侧新增设置入口；设置页在原窗口内容区切换，保持 901×513 最小尺寸、统一比例缩放和无滚动条。
- 主题偏好扩展为跟随 Windows、固定日间、固定夜间；窗口重新激活时会在系统模式下复核 Windows 主题。
- 新增“启动时自动检查新版本”开关和手动“立即检查”；仍只读取 GitHub Release 版本号，不在客户端内
  自动下载、安装或执行更新。
- `multi_instances` 恢复加入外部冲突保护：保存原始状态和助手临时状态，恢复前比较存在性、值和类型；
  第三方程序在会话期间写入的新状态不再被旧快照覆盖。
- 自动测试新增双客户端页脚计数、设置页导航与控件、更新偏好持久化，以及双开/三开两类注册表外部写入
  冲突用例。
- 新增 GitHub Actions 干净 Windows Server 2022 工作流：Release 重建、完整测试、组包、SHA-256 逐项
  复核、Microsoft SBOM Tool 4.1.2 的 SPDX 2.2 SBOM 生成与验证、构建工件上传。
- GitHub 官方 Actions 均固定到明确提交哈希，工作流仅有 `contents: read` 权限。
- UI 回归增加主设置页日间/夜间场景，矩阵扩展到 12 个自动截图。
- 更新 README、发布说明、安全审计、验证报告、便携版说明、发布清单和 Kaspersky 最终扫描门禁。

## V1.0.4 — 2026-09-14

- 将用户提供的 901×513 主界面定为最低窗口尺寸，删除主内容外层滚动容器，最小尺寸即可完整操作。
- 取消窄屏上下堆叠断点，微信/企业微信保持左侧，目标窗口数在所有尺寸下保持最右侧。
- 新增统一比例界面画布；窗口放大或最大化时，文字、图标、按钮、输入框、圆角、阴影和间距同步缩放，
  左右卡片再按 2.4:1 分配额外横向空间，覆盖 3440×1392 超宽屏而不留下大块空白。
- 新增 `IWeComLaunchPolicy`，把企业微信注册表兼容设置限制在一次启动会话内并确保 `Dispose/finally` 恢复。
- 双开时临时使用官方 `multi_instances=2` 提示；三开及以上时临时移除这条只支持双开的提示，
  逐次释放精确匹配的 `Tencent.WeWork.ExclusiveObject*` 互斥锁后启动缺少实例。
- 真机验证企业微信 `WXWork.exe 5.0.11.6018`：直接写 `multi_instances=3` 只能得到 2 个根实例；
  临时无注册表提示并释放已知锁后成功达到 3 个根实例，测试窗口随后正常关闭，原注册表状态恢复。
- 企业微信每次释放锁后增加 100ms 交接等待，检测到新实例后增加 800ms 稳定等待，降低连续启动竞争。
- 本地诊断增加企业微信双开/扩展模式、注册表恢复行为和已验证版本基线说明。
- 自动测试新增企业微信注册表原值/类型恢复、缺失值不残留、1→3 增量编排和时序断言；UI 回归覆盖
  901×513、1280×720、1920×1080、3440×1392 与双主题。
- 按项目决定不推进代码签名；继续公开源码标签、SHA-256、完整安全审计、UI 证据与 Kaspersky 扫描日志。

## V1.0.3 — 2026-09-13

- 覆盖安装前精确检测目标目录中的助手进程，先询问正常关闭，超时后再单独询问是否强制关闭；默认不强制。
- 安装器只处理该安装路径中的助手，不结束微信、企业微信或其他目录中的同名程序。
- 主窗口支持 680×520 最小尺寸、自由缩放和最大化；窄屏纵向排列，宽屏保持数量卡片位于最右侧。
- 主程序、安装器和清理器加入 Per-Monitor V2 DPI 清单，文字切换为 WPF 理想排版。
- 新增手动选择客户端入口，并以文件名、产品资源和有效腾讯 Authenticode 签名三重验证微信/企业微信。
- 真机验证微信 4.1.15.6 与企业微信 5.0.11.6018，继续兼容 `WeChat.exe`、`Weixin.exe` 与 `WXWork.exe`。
- 新增低干扰“诊断”按钮，报告只写入程序目录下的 `diagnostics`，不会自动上传。
- 新增 GitHub Release 自动版本检测；只读取公开版本号，更新下载必须由用户在浏览器中完成。
- 将进程枚举、父子关系、启动和等待抽象为 `IProcessEnvironment`，加入确定性测试环境。
- 安装器与清理器改为不同程序集、不同产品身份和不同二进制，安装器内嵌真正的独立清理器。
- 自动回归矩阵扩展至 5 种主窗口尺寸、双主题、安装完成态与独立清理器。
- 完成代码签名费用、SignPath 开源方案、WiX/MSI 和 UI 框架路线评估；本版保留 WPF 与自有安装器。

## V1.0.2 — 2026-09-13

- 主程序、安装器和完全卸载器由 WinForms/GDI+ 自绘迁移为 WPF 矢量界面。
- 日间与夜间主题改用共享资源字典；卡片阴影使用独立柔化层，避免文字和图标一起模糊。
- 三个窗口继续使用 Windows 原生标题栏，支持拖动、最小化、关闭和系统 DWM 外观。
- 安装完成后不再自动启动应用；安装器进入明确完成态，仅在用户点击“确认并启动”后运行。
- 多开、定位、计数、缓存和句柄处理拆分为独立 `WechatDuokai.Core` 类库。
- 主程序、Core、安装器和测试统一为 .NET Framework 4.8 x64，未增加第三方 UI/NuGet 依赖。
- 绿色版增加必需的 `WechatDuokai.Core.dll`，安装器同时嵌入并释放该 DLL。
- 新增架构评估与 10 个代表性 GitHub 多开项目的详细对比报告。
- 自动验证增加明确启动确认、WPF 原生窗口、主题入口和内嵌 Core 完整性检查。
- 更新发布脚本、README、绿色版说明、界面截图、校验文件和安全复检流程。

## V1.0.1 — 2026-09-13

- 安装地址改为通过 Windows 文件夹选择器设置，路径框只读，无需手工输入。
- 自定义安装目录加入安全校验，拒绝磁盘根目录、关键系统目录、目录联接、源码/发布目录和未标记的非空目录。
- 主程序、安装器与完整卸载器统一为简约视觉体系，支持日间/夜间主题切换并保存选择。
- 恢复 Windows 原生标题栏，三个窗口均可可靠拖动、最小化和关闭。
- 数量控件改为独立的减号、数字输入框和加号，仍位于界面最右侧并自动保存。
- 新增《旧版 EXE 硬编码路径说明与风险评估报告》TXT 文档。
- 扩展审计上游源码、旧版 EXE、当前二进制、构建链、运行端点、资源和安装/卸载删除边界。
- 公开记录并调查一次 `VHO:Trojan.Win32.Convagent.gen` 中间构建启发式/云端告警及证据限制。
- 进程路径校验改为失败关闭；`rundll32` 兼容逻辑只允许精确锁文件匹配。
- 完全卸载拒绝链接根目录，递归清理不跟随目录联接/符号链接，并校验清理计划身份。
- 构建脚本支持按版本号生成安装版、绿色版、清理器、清单与 SHA-256 文件。
- README 扩展为完整的下载、使用、架构、安全、构建、兼容性与贡献文档。
- 自动验证覆盖数量缓存、安装路径、可移动窗口、内嵌载荷、互斥锁和文件锁等 9 组场景。

## V1.0.0 — 2026-09-12

- 自动保存上一次设置的目标实例数量（1–10）。
- 增加运行实例检测；再次点击只补足缺少的数量，不要求退出其余窗口。
- 同时兼容旧版微信命名互斥锁、微信 4.x `lock.ini` 文件锁，以及企业微信独占互斥锁。
- 重做 WinForms 界面：微信与企业微信图标位于左侧，目标数量与输入框位于最右侧。
- 增加安装版、绿色免安装版、SHA-256 校验文件和一键发布脚本。
- 增加两档完全卸载：保留源码，或在多重安全校验与确认后连同源码删除。
- 移除旧界面的第三方站点跳转，源码链接改为原始 GitHub 仓库。
- 完成源码与原始二进制的静态安全审计，详见 `SECURITY-AUDIT.md`。
