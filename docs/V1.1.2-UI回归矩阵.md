# V1.1.2 UI 回归矩阵

V1.1.2 不改动主界面布局、颜色、Logo、窗口最小尺寸或比例缩放。唯一可见文案变化是设置页把
“启动时自动检查新版本”改为“每天自动检查新版本”，准确反映 24 小时缓存与错峰策略。

| 界面 | 日间 | 夜间 | 901×513 最小 | 1920×1080 / 最大化 | 键盘与可访问名称 |
| --- | --- | --- | --- | --- | --- |
| 主界面 | 保持 V1.1.0 基线 | 保持低亮度深色基线 | 无外层滚动条、无裁切 | 等比填满可用区域 | 保持 |
| 设置常规页 | 新文案完整 | 新文案完整 | 主题、诊断、更新区域可见 | 比例缩放 | 保持 |
| 软件介绍页 | 保持 | 保持 | 无裁切 | 比例缩放 | 保持 |
| 更新确认框 | 按核验来源显示清单/API 文案 | 系统对话框主题行为保持 | 信息完整 | 不依赖窗口尺寸 | Yes/No 默认安全 |

自动检查的 30–300 秒等待发生在窗口加载完成后，不锁住 UI 线程；用户仍可立即操作多开、切换设置、
手动检查更新或关闭程序。手动检查不显示缓存结果，而是立即请求最新状态。
